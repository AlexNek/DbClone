<Window x:Class="DbClone.UI.Views.BackupPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:views="clr-namespace:DbClone.UI.Views"
        Title="{Binding Title}"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="{Binding Description}"
                   FontSize="13" TextWrapping="Wrap" Margin="0,0,0,12"/>

        <!-- Password field -->
        <StackPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Password" FontSize="12" Margin="0,0,0,4"/>
            <views:SyncablePasswordBox
                Password="{Binding Password, Mode=TwoWay}"
                RevealButtonEnabled="True"
                AutomationProperties.Name="Backup Password"/>
        </StackPanel>

        <!-- Confirm password field (export mode only) -->
        <StackPanel Grid.Row="2" Margin="0,0,0,8"
                    Visibility="{Binding IsConfirmVisible, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Confirm Password" FontSize="12" Margin="0,0,0,4"/>
            <views:SyncablePasswordBox
                Password="{Binding ConfirmPassword, Mode=TwoWay}"
                RevealButtonEnabled="True"
                AutomationProperties.Name="Confirm Password"/>
        </StackPanel>

        <!-- Error message -->
        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}"
                   Foreground="Red" FontSize="12" TextWrapping="Wrap"
                   Margin="0,4,0,0">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Footer buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <ui:Button Content="Cancel" Width="80" Height="32" Margin="0,0,8,0"
                       Click="CancelClick"/>
            <ui:Button Content="OK" Width="80" Height="32"
                       Appearance="Primary"
                       Click="OkClick"/>
        </StackPanel>
    </Grid>
</Window>
