<UserControl
    x:Class="DbClone.UI.Views.ObjectsPanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DbClone.UI"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:DbClone.UI.ViewModels">

    <UserControl.Resources>
        <local:ObjectStatusToSymbolConverter x:Key="ObjectStatusToSymbolConverter" />
    </UserControl.Resources>

    <!-- Compact horizontal strip -->
    <Border Padding="4,2">
        <ItemsControl ItemsSource="{Binding Items}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:ObjectItem}">
                    <Border Padding="8,3"
                            Margin="2,0"
                            CornerRadius="4"
                            Background="{ui:ThemeResource ControlFillColorSecondaryBrush}"
                            ToolTip="{Binding Description}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:SymbolIcon
                                Width="14"
                                Height="14"
                                FontSize="11"
                                VerticalAlignment="Center"
                                Symbol="{Binding Status, Converter={StaticResource ObjectStatusToSymbolConverter}}"
                                Margin="0,0,4,0">
                                <ui:SymbolIcon.Style>
                                    <Style TargetType="ui:SymbolIcon">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="{x:Static vm:EObjectStatus.Failed}">
                                                <Setter Property="Foreground" Value="Red" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ui:SymbolIcon.Style>
                            </ui:SymbolIcon>
                            <TextBlock Text="{Binding DisplayName}" 
                                       FontSize="11" 
                                       VerticalAlignment="Center"
                                       Margin="0,0,4,0"/>
                            <Border Background="{ui:ThemeResource AccentTextFillColorPrimaryBrush}"
                                    CornerRadius="6"
                                    Padding="4,0"
                                    VerticalAlignment="Center">
                                <TextBlock Text="{Binding Count}" 
                                           FontSize="9" 
                                           FontWeight="SemiBold"
                                           Foreground="White"/>
                            </Border>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</UserControl>
